{
  "name": "jiran",
  "private": true,
  "description": "Réseau social de quartier pour l'Algérie",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix mobile",
    "essai": "node scripts/essai.mjs",
    "test": "npm test --prefix server && npm test --prefix mobile"
  }
}
